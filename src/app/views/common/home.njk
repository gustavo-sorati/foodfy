{%extends 'layout.njk' %}

{% block css%}
<link rel="stylesheet" href="styles/home.css">
<link rel="stylesheet" href="styles/recipes-grid.css">
{% endblock%}

{% block content %}
<section class="container home">
    <section class="banner">
            <div class="banner-info">
                <div class="banner-message">
                    <h1 class="title">As melhores receitas</h1>
                    <p>Aprenda a contrutir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
                </div>
            

                <div class="banner-img">
                    <img src="/assets/chef.png" alt="">
                </div>
            </div>
    </section>

    <section class="management-wrapper container">
        <div class="management">
            <h1 class="title">Melhores receitas</h1>

            {% for recipe in recipes%}
                {% if loop.index0 < 4 %}
                <div class="recipes">
                    <img src="{{recipe.image}}" alt="{{recipe.title}}">
                    
                    <div class="info">
                        <h3>{{recipe.title}}</h3>
                        <p>por {{recipe.author}}</p>
                        {# <a href="/recipes/{{recipe.id}}">Visualizar</a> #}
                    </div>     
                </div>
                {% endif %}
            {% endfor %}

        </div>
    </section>

</section>
{% endblock %}

{% block scripts%}
<div class="modal">
    <div>
        <img src="">
        <h1></h1>
        <p></p>
        <a class="close-modal">Fechar modal</a>
    </div>
</div>

<script src="scripts/modalController.js"></script>
{% endblock %}