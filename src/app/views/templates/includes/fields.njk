<div class="card">
    <div class="title">Nome</div>

    <input 
        type="text"
        id="name"
        name="name"
        value="{{chef.name}}"
    >
</div>

{# IMAGE #}
<div class="card">
    <div class="title">Avatar</div>

    <input 
        type="text"
        id="image"
        name="avatar_url"
        value="{{chef.avatar_url}}"
    >
</div>
